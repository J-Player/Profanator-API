spring:
  r2dbc:
    url: r2dbc:postgres://localhost:5432/profanator_db
    username: postgres
    password: admin
logging:
  level:
    org:
      springframework:
        r2dbc: INFO
        cache: DEBUG
    api:
      config:
        cache: DEBUG
      service:
        cache: DEBUG
server:
  port: 8080
  error:
    include-stacktrace: on_param