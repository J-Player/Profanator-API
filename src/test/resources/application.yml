spring:
  sql:
    init:
      data-locations: # Purposely null property to ignore data.sql on startup
  r2dbc:
    url: r2dbc:h2:mem:///testdb?options=MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
logging:
    api:
      configs: DEBUG
      services:
        cache: TRACE