spring:
  r2dbc:
    url: r2dbc:postgres://localhost:5432/profanator_db
    username: postgres
    password: admin
logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG
        cache: TRACE
server:
  port: 8080
  error:
    include-stacktrace: on_param
app:
  config:
    cache:
      ttl: 60s